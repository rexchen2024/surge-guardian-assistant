[project]
name = "surge-guardian-assistant"
version = "0.2.0"
description = "Lightweight autonomous Surge guardian assistant with Hermes and Codex editions"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
