[project]
name = "surge-hermes-guardian"
version = "0.1.0"
description = "Lightweight autonomous Surge guardian for Hermes cron"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

