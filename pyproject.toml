[project]
name = "surge-sentry"
version = "0.2.0"
description = "Lightweight autonomous Surge Sentry assistant with Hermes and Codex editions"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
